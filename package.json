{
  "name": "banca-dg",
  "version": "2.0.0",
  "private": true,
  "description": "Sistema de venta de numeros de loteria (banca dominicana)",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node src/db/seed.js",
    "salud": "node -e \"require('./src/db').comprobar().then(r=>{console.log(r);process.exit(0)}).catch(e=>{console.error(e.message);process.exit(1)})\"",
    "usuarios": "node src/db/usuarios.js"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
